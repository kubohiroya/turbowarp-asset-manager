{
  "name": "@kubohiroya/turbowarp-asset-manager",
  "version": "0.4.1",
  "description": "An IndexedDB-backed image, audio, and runtime-text asset manager for TurboWarp projects.",
  "author": "Hiroya Kubo <hiroya@cuc.ac.jp>",
  "type": "module",
  "license": "MPL-2.0",
  "homepage": "https://github.com/kubohiroya/turbowarp-asset-manager#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kubohiroya/turbowarp-asset-manager.git"
  },
  "bugs": {
    "url": "https://github.com/kubohiroya/turbowarp-asset-manager/issues"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "unpkg": "./dist/asset-manager.js",
  "jsdelivr": "./dist/asset-manager.js",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "packageManager": "pnpm@11.11.0",
  "scripts": {
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "test": "vitest run",
    "docs": "node scripts/generate-readme.mjs",
    "docs:check": "npm run docs && git diff --exit-code -- README.md",
    "docs:site:check": "node scripts/check-site.mjs",
    "check:dist": "npm run build && git diff --exit-code -- dist",
    "pack:check": "npm pack --dry-run --ignore-scripts",
    "release:check": "npm publish --dry-run --access public",
    "check": "npm run typecheck && npm run test && npm run build && npm run docs:check && npm run docs:site:check && npm run check:dist && npm run pack:check",
    "prepack": "npm run check"
  },
  "devDependencies": {
    "@kubohiroya/vite-plugin-turbowarp-extension": "0.1.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.6",
    "vitest": "^4.1.10"
  },
  "keywords": [
    "turbowarp",
    "scratch",
    "extension",
    "assets",
    "indexeddb"
  ]
}
