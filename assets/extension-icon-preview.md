# Preview

The canonical artwork is `extension-icon.svg`.

For a local PNG preview, render the SVG at 600 × 300 pixels. The source is designed for lossless rasterization and uses only vector geometry.
