## Unreleased

- Rename the actor animation list argument from `COSTUMES` to `ASSETS` in the loop and sequence block definitions.
- Clarify that animation entries are registered image asset names.
- Continue accepting the legacy `COSTUMES` argument in saved projects.
